#spring.application.name=vetApp
spring:
  application:
    name: vetApp
  datasource:
    url: jdbc:mysql://localhost:3307/vet_app_db
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 9000